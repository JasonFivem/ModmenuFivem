local modMenu = {}

modMenu.menuActive = false

function modMenu:toggleMenu()
    if self.menuActive then
        self.menuActive = false
    else
        self.menuActive = true
    end
end

function modMenu:drawMenu()
    if self.menuActive then
        -- draw menu
    end
end

function modMenu:banPlayer()
    -- ban player logic
end

function modMenu:kickPlayer()
    -- kick player logic
end

return modMenu
